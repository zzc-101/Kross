# SaaS Work Agent 本地部署
KROSS_PORT=8787
KROSS_POSTGRES_PASSWORD=
# 用于加密组织模型 API Key 和 SSO Client Secret 的主密钥，本地由 start-cloud.sh 自动生成
KROSS_CREDENTIAL_MASTER_KEY=
# 1 仅用于跳过登录的开发/冒烟测试。默认走账号密码；企业 SSO 在管理中心配置，不走环境变量。
KROSS_DEV_IDENTITY=0
KROSS_ORCHESTRATOR_MANAGER_ID=kross-saas
# 单机可保持 http://kross-server:8787（Worker 走 Docker 网络直连控制面）。
# 集群由 Helm 把 KROSS_PUBLIC_BASE_URL 设为控制面 Service DNS。
KROSS_PUBLIC_BASE_URL=http://kross-server:8787

# Java 控制面日志同时输出到 Docker stdout 和宿主机目录；文件按大小与日期轮转
KROSS_LOG_DIR=./runs/logs/server
KROSS_LOG_MAX_FILE_SIZE=50MB
KROSS_LOG_MAX_HISTORY=14
KROSS_LOG_TOTAL_SIZE_CAP=1GB

KROSS_S3_PORT=9000
KROSS_S3_ACCESS_KEY=kross
KROSS_S3_SECRET_KEY=
KROSS_S3_BUCKET=kross
KROSS_S3_PUBLIC_ENDPOINT=http://127.0.0.1:9000

# Redis 缓存（身份/鉴权/模型/Skill 目录热点读）。设为 false 可完全关闭缓存层，
# 控制面会直接回源 PostgreSQL；集群部署建议保持开启。
KROSS_CACHE_ENABLED=true

# 工作区存储：local=本机 Docker volume（默认）。集群 k3s 由 Helm 设为 juicefs。
KROSS_WORKER_STORAGE=local
# 运行时：local=控制面本机 Docker（默认）；kubernetes=由 k3s 调度 Worker Pod。
# 单机脚本只接受 local。多机请 helm install deploy/cluster。
KROSS_WORKER_RUNTIME=local

# 知识库（可选）。默认不启动 knowledge 服务。
# 本地启用：./scripts/start-cloud.sh --knowledge
# 然后在管理端「平台设置」打开知识库开关，并在「知识库」上传 Markdown、PDF、Word 或图片。
# Postgres 需使用带 pgvector 的镜像（compose/helm 已换成 pgvector/pgvector:pg17）。
# 解析不走模型：PDF 抽文字层，Word 抽段落/表格。扫描版 PDF 暂无 OCR。
# 向量模型默认 tencent/WeMM-Embedding-2B。无 GPU 且未配置远程向量服务时不能启用。
# PROVIDER=auto：有 BASE_URL 走 http，有 GPU 走 local，否则拒绝启用。
# SIZE=2b/4b/9b 换规格；MODEL 可换成其他向量模型。
KROSS_KNOWLEDGE_INTERNAL_TOKEN=
KNOWLEDGE_EMBEDDING_PROVIDER=auto
KNOWLEDGE_EMBEDDING_BASE_URL=
KNOWLEDGE_EMBEDDING_API_KEY=
KNOWLEDGE_EMBEDDING_SIZE=2b
KNOWLEDGE_EMBEDDING_MODEL=
KNOWLEDGE_EMBEDDING_DIM=0
KNOWLEDGE_EMBEDDING_MODEL_DIR=
KNOWLEDGE_EMBEDDING_HUB=huggingface
KNOWLEDGE_EMBEDDING_DEVICE=auto

# LLM example (choose one provider)
AGENT_LLM_PROVIDER=openai
AGENT_LLM_MODEL=gpt-5.6-sol
OPENAI_API_KEY=
OPENAI_BASE_URL=
