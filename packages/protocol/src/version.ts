/**
 * Cloud wire protocol version.
 *
 * Version 2 intentionally replaces the workspace/session-oriented Cloud v1
 * contract. Local TUI persistence is not part of this protocol.
 */
export const PROTOCOL_VERSION = 2 as const;

