# 2026-08-24 后端审查

范围：`backend/` 全量（176 个 Java 文件），覆盖安全与身份（security / identity /
channel token）、编排与多机（orchestrator / fleet / catalog / storage）、核心
Agent 链路（agent / channel / controller）三条线。结论来自代码审查，按严重度
排列。本轮仅记录，暂不修改。

## 总体评价

做得好的：session fixation 防护；token 只存哈希 + 邀请码 CAS 原子核销；
bootstrap 行锁 + partial unique index 双防竞争；AES-GCM 用法正确（随机 IV、
128-bit tag）；MyBatis 全参数化，无 SQL 注入面；Docker 名称经 SHA-256 派生、
不经 shell；Skill 包路径穿越校验正确；job 认领 `FOR UPDATE SKIP LOCKED`
无竞态；属主校验全面，未发现绕过 `OrganizationAccess.require` 的越权路径。

## 安全

| 严重度 | 位置 | 问题 | 状态 |
|---|---|---|---|
| High | `SsoService.java:147` | `email_verified` 缺省（null）时被 `!Boolean.FALSE.equals(null)` 视为已验证，随后按邮箱自动绑定本地账号。IdP 不下发该 claim 时（如自建 Keycloak 默认配置），冒用他人邮箱即可账号接管。应要求显式 `== true` 才允许绑定。 | 待修 |
| High | `IdentityFilter.java:51` | `KROSS_DEV_IDENTITY=true` 时请求头 `x-kross-user-id` 可伪造任意 userId 直接获得身份并 upsert 用户。无 profile / 环境约束，生产误配等于完全绕过认证。 | 待修 |
| High | `IdentityFilter.java:36` | 身份完全来自 HttpSession 缓存的 `Identity`（含 `platformRole`），不回查 DB。封禁用户、super_admin 被降权、改密后：组织资源因实时查 membership 立即失效（好），但平台级接口最长存活 7 天（session timeout）。建议 session 只存 userId 或引入吊销版本号。 | **已修**：每次请求按 userId 回查 active 用户并刷新身份；禁用立即清会话 |
| Medium | `SecurityConfig.java:29` | CSRF 全关且认证全靠 cookie。SameSite=Lax 挡不住旧浏览器与同站子域场景。至少对状态变更加 Strict 或 CSRF token。 | 待修 |
| Medium | `OidcClient.java:93` | ID token 只接受 RSA 签名算法族，ES256/EdDSA 的 IdP 登录失败。建议按 discovery 文档的 `id_token_signing_alg_values_supported` 白名单。 | 待修 |
| Medium | `OidcClient.java:150` / `SsoService.java:260` | issuer 允许 `http://`（discovery/token 明文可被 MITM）；redirect_uri 信任 Host 头，前置代理未固定 Host 时可被操纵（受 IdP 注册白名单缓解）。 | 待修 |
| Medium | `AuthService.java:99` | 登录无限速/锁定：查无此人跳过 BCrypt（时序侧信道可枚举用户名）；禁用账号在验密前抛错（未持凭证即可探测账号状态）。建议不存在用户也跑 dummy bcrypt。 | **部分修复**：不存在用户执行 dummy BCrypt，禁用/SSO 状态只在验密成功后返回；限速仍待实现 |
| Low | `PasswordConfig.java:11` / `AuthCredentials.java:11` | BCrypt strength=10 偏低（建议 ≥12 或 Argon2id）；密码上限 128 字符超过 BCrypt 72 字节截断上限，长密码静默截断。 | **部分修复**：新密码拒绝超过 72 UTF-8 字节；算法强度升级仍待实现 |
| Low | `CredentialVault.java:38` | 主密钥仅单轮无盐 SHA-256 即作 AES key，低熵口令下离线爆破成本低。建议 HKDF/scrypt 并记录 key version 支持轮换。 | 待修 |

## 编排 / 多机

| 严重度 | 位置 | 问题 | 状态 |
|---|---|---|---|
| High | `NodeFleetBackend.java:40` | cluster 换节点时旧节点 stop 是 best-effort 且异常被吞。旧容器仍持有效 agent token 可继续 claim/postReply，JuiceFS 共享工作区双容器读写 → 数据损坏 + 重复执行。stop 失败应为硬失败或引入租约 fencing。 | **已修**：旧节点离线或 stop 失败时拒绝漂移，只有确认停止才启动新实例 |
| High | `AgentService.wake()`（:853 起） | inspect→start 无 per-agent 锁，appendMessage afterCommit / scheduleWake / ensureWorker 可并发触发。local 模式 start 带 force 强删同名旧容器，会误杀刚启动的容器；并发首启 ensureVolume 也可能双双走到 create 抛 Conflict。 | **已修**：volume、wake 与 sleep 使用同一 per-agent runtime lock 串行化 |
| High | `NodeHub.detach`（:79） | 节点重连时旧 session 关闭回调无条件 `markOffline(nodeId)`，把刚 attach 成功的新连接标死；下次心跳（10s）前调度器会把该节点 agent 全部漂走，原节点容器仍在跑——与上一条叠加成双活。detach 应校验 session 归属。 | **已修**：仅当前 session 成功从映射移除时才标记离线 |
| Medium | `CredentialVault.java:60` | AES-GCM 未绑 AAD（credentialId/modelId），同一主密钥下密文可互换：能写库者可把 A 组织凭据密文替换进 B 组织记录而解密成功。 | 待修 |
| Medium | `NodeHub.java:29` | sessions / 在线判定是进程内 Map，控制面横向扩容即脑裂：各副本只见连到自己的节点，任一副本的 `markStaleOffline` 可把其他副本的活跃节点判死。当前隐含单实例假设但未强制。 | 待修 |
| Medium | `NodeHub.pickLeastLoaded`（:100） | 取在线列表第一个元素，`runningAgents` 采集了却不参与选择，负载全压到同一节点。 | 待修 |
| Low | `NodeWebSocketHandler.java:56` | Result 处理只按 requestId 匹配 pending future，不校验回结果的 nodeId 是否为命令目标；持任一 node token 可伪造其他节点的结果。requestId 为随机 UUID，实际利用门槛高。 | **已修**：pending request 绑定目标 nodeId，非目标节点结果不完成请求 |
| Low | `SkillPackageArtifactService.safePath`（:150） | 只校验路径文本，未拒 symlink/hardlink 类型 ZipEntry。服务端只做内容寻址存储，解压在 worker 侧，若 worker 解压未防护存在 zip-slip 残余风险。inspect 时应拒绝非普通文件 entry。 | 待修 |
| Low | `SecurityConfig.java:39` / `IdentityFilter.java:31` | `/internal/v2/**` 整段 permitAll + 过滤器跳过。当前仅两个自带 token 握手的 WS 端点，无直接利用面，但未来挂在 `/internal/` 的 HTTP controller 将完全无鉴权，属高危配置惯性。 | 待修 |
| Low | `DockerContainerBackend.java:103` | agent 明文 token 经环境变量注入容器（同宿主机 `docker inspect` 可读，TTL 默认 12h）；cluster 下还经 WS 明文下发 StartCommand，链路必须 TLS。 | 待修 |

## 核心 Agent 链路

| 严重度 | 位置 | 问题 | 状态 |
|---|---|---|---|
| High | `AgentMapper.xml:247` / `AgentService.reconcileRuntimeAgents` | stuck processing 无看门狗：worker 进程假死但容器仍 running 时，消息永久停在 `processing` → `hasProcessing` 恒真 → `claimIfIdle` 永远返回空，该用户后续所有消息无法处理。缺基于时间的超时回收。 | 待修 |
| High | `AgentMemoryService.java:112` | `@Transactional` 的 `appendMessage` 内同步发两条 `workspace.write` WS 命令（各 15s 超时）：DB 连接占用最长 ~30s；文件写入先于提交，事务回滚则工作区已改而 DB 无记忆，不一致。同类：`updateMcpConfig` 的 `mcp.save`（45s）。 | 待修 |
| High | `ChannelEventBus.remove`（:49） | 先移除 emitter 再判断删 key，与 `computeIfAbsent` 并发时可把含新订阅者的 list 整个从 map 删除 → 新 SSE 订阅者收不到任何事件且永不清理。另 `SseEmitter(0L)` 无超时，半关连接的 emitter 可能永久驻留。 | 待修 |
| High | `AgentSocketHub.offerJob`（:137） | `synchronized(state.lock)` 内做 DB 事务（claimIfIdle）+ 心跳/审批转发全部被阻塞；Java 21 下长时间 synchronized 阻塞 pin carrier 线程。`consolidateIfDue` 持锁 75s 更甚。改 ReentrantLock + 缩小临界区。 | 待修 |
| High | `AgentService.modelEnvironment`（:650） | 不做组织过滤：持任意合法 agent token 即可解密获取**任意** active 平台模型的 API Key。多租户下跨组织凭证泄露。`resolveUsableModel` 应加 org 过滤。 | 待修 |
| Medium | `AgentService.resolveApproval`（:223） | approvalId 直接透传 worker：不校验属于该会话、不防重放（重复 approve）、决策不入库（断线即丢，危险操作无审计追溯）。 | 待修 |
| Medium | `AgentService.postReply` / `ingestEvents`（:520） | worker 上行 payload 无大小/条数上限（WS 文本缓冲 2MB），直接写 jsonb 并全量广播 SSE，存储膨胀 + 放大 DoS 面。用户侧有 32KB 限制，worker 侧没有。 | 待修 |
| Medium | `V1` 迁移 / mapper 查询 | 索引缺失：`status='processing'` 无部分索引（每次调度/心跳全表扫）；`listUserMessagesSince` 的 `agent_id+role+created_at` 无法用以 organization_id 打头的 timeline 索引；`usageCounts` 整表 FILTER 聚合无 `(organization_id, created_at)` 索引。 | 待修 |
| Medium | `AgentSocketHub.attach`（:59) | 重连无条件 `busy=false`：旧连接作业刚 completeMessage 提交、尚未 markIdle 时重连，register→offerJob 立即派发新 job，与新连接处理的旧 turn 并发。 | 待修 |
| Low | `AgentMemoryService.extractLocks`（:54） | 每 agent 锁对象常驻 map，永不清理；配合锁内阻塞会堆积等待中的虚拟线程。 | 待修 |
| Low | `AgentSocketHub.awaitConnected`（:96） | waiter 可被加进已被 signalConnected 摘除的旧 list 白等满超时；detach 只 failPending 不唤醒 connectWaiters。 | 待修 |
| Low | `AgentService.agentMessage`（:544） | `agentMessageId` 只校验 org+agentId 不校验同会话，worker 可跨会话注入内容（限同一用户范围，影响有限）。 | 待修 |
| Low | `AgentMapper.xml listHistory`（:132） | 仅按 conversationId 查询，无组织过滤，与其余 `(organization_id, id)` 双键风格不一致。 | 待修 |
| Low | `AgentMemoryMapper.listForgottenContents` | 无 LIMIT 全量拉取后在 Java 截 80 条，数据量大时浪费。 | 待修 |
| Low | `AgentService.appendMessage`（:201） | afterCommit 回调里同步 wake()，容器冷启动可达数十秒，拖住发首条消息的 HTTP 响应。建议异步化。 | 待修 |

## 与 2026-08-21 审查的衔接

- 上轮已修项（SSO 仅按已验证邮箱绑定、bootstrap 唯一超管、reconcile 与 wake
  解耦、starting 宽限等）复核有效。注意：上轮「SSO 按 email 绑定」的修复仍
  存在 `email_verified == null` 视为已验证的缺口，见安全表第一条。
- 上轮「公网 Nginx 不反代 /internal/」缓解了本轮回第 9 条低危配置惯性的直接
  利用面，但 Spring 侧 permitAll 未收紧。

## 建议优先顺序

1. 一行修复类：SsoService 要求 `email_verified == true`；IdentityFilter 给 dev
   身份头加 `@Profile("dev")`；modelEnvironment 加组织过滤。
2. 稳定性类：stuck-processing 时间看门狗；wake()/ensureVolume 加 per-agent 锁;
   ChannelEventBus 移除竞态改 compute 语义。
3. 架构类：fleet stop 失败 fencing + detach session 校验;offerJob 锁外化 +
   去 synchronized；session 回查 DB 或吊销版本号。
